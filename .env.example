# Copy this file to `.env` and fill in real values.
# `.env` is gitignored — never commit secrets.

# Anthropic API key — required from arch_01 onward (arch_00 has zero Claude calls).
ANTHROPIC_API_KEY=

# Qdrant connection. Defaults to the docker-compose service exposed on localhost.
QDRANT_URL=http://localhost:6333
